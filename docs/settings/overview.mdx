---
hide_title: true
hide_table_of_contents: true
id: overview
title: Settings
sidebar_label: Overview
---

import Link from "@docusaurus/Link";

import { LuSettings, LuBuilding2, LuStore, LuUser, LuFileCheck } from "react-icons/lu";

<div style={{display:'flex',alignItems:'center',gap:'10px',marginBottom:'6px'}}>
  <LuSettings size={28} style={{color:'var(--ifm-color-primary)',flexShrink:0}} />
  <h1 style={{margin:0}}>Settings</h1>
</div>

Programmatically read and update your RM configuration — accounts, stores, merchant profile, and user details.

---

## Configuration APIs

<div className="ov-grid-auto">

<Link to="./account-detail/get-accounts" className="ov-card--link">
  <div className="ov-card">
    <div className="ov-card-icon"><LuFileCheck size={22} /></div>
    <span className="ov-card-title">Account</span>
    <span className="ov-card-desc">Create, retrieve, update accounts, and submit for RM review.</span>
  </div>
</Link>

<Link to="./store-detail/store-details" className="ov-card--link">
  <div className="ov-card">
    <div className="ov-card-icon"><LuStore size={22} /></div>
    <span className="ov-card-title">Store</span>
    <span className="ov-card-desc">Create, update, and delete stores. Fetch store details by merchant or ID.</span>
  </div>
</Link>

<Link to="./merchant-detail/merchant-profile" className="ov-card--link">
  <div className="ov-card">
    <div className="ov-card-icon"><LuBuilding2 size={22} /></div>
    <span className="ov-card-title">Merchant</span>
    <span className="ov-card-desc">Retrieve merchant profile and active subscription information.</span>
  </div>
</Link>

<Link to="./user-profile" className="ov-card--link">
  <div className="ov-card">
    <div className="ov-card-icon"><LuUser size={22} /></div>
    <span className="ov-card-title">User</span>
    <span className="ov-card-desc">Fetch the profile of the authenticated user.</span>
  </div>
</Link>

</div>

<!-- SPDX-License-Identifier: Apache-2.0 -->
