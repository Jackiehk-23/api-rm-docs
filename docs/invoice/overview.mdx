---
hide_title: true
hide_table_of_contents: true
id: overview
title: Invoice
sidebar_label: Overview
---

import Link from "@docusaurus/Link";

import { LuFileText, LuPlus, LuList, LuSearch, LuPencil, LuBan } from "react-icons/lu";

<div style={{display:'flex',alignItems:'center',gap:'10px',marginBottom:'6px'}}>
  <LuFileText size={28} style={{color:'var(--ifm-color-primary)',flexShrink:0}} />
  <h1 style={{margin:0}}>Invoice</h1>
</div>

Create and manage invoices — bill customers with a shareable payment link, list and look up invoices, edit pending ones, and cancel when needed.

---

<div className="ov-grid-auto">

<Link to="./create-invoice" className="ov-card--link">
  <div className="ov-card">
    <div className="ov-card-icon"><LuPlus size={22} /></div>
    <span className="ov-card-title">Create Invoice</span>
    <span className="ov-card-desc">Create a new invoice and get a shareable payment URL.</span>
  </div>
</Link>

<Link to="./get-invoices" className="ov-card--link">
  <div className="ov-card">
    <div className="ov-card-icon"><LuList size={22} /></div>
    <span className="ov-card-title">Get Invoices</span>
    <span className="ov-card-desc">List invoices with filtering by store, status, and date range.</span>
  </div>
</Link>

<Link to="./get-invoice-by-id" className="ov-card--link">
  <div className="ov-card">
    <div className="ov-card-icon"><LuSearch size={22} /></div>
    <span className="ov-card-title">Get Invoice by ID</span>
    <span className="ov-card-desc">Retrieve a single invoice by its ID.</span>
  </div>
</Link>

<Link to="./edit-invoice" className="ov-card--link">
  <div className="ov-card">
    <div className="ov-card-icon"><LuPencil size={22} /></div>
    <span className="ov-card-title">Edit Invoice</span>
    <span className="ov-card-desc">Update a pending invoice's details before payment.</span>
  </div>
</Link>

<Link to="./cancel-invoice" className="ov-card--link">
  <div className="ov-card">
    <div className="ov-card-icon"><LuBan size={22} /></div>
    <span className="ov-card-title">Cancel Invoice</span>
    <span className="ov-card-desc">Cancel a pending invoice (status becomes EXPIRED).</span>
  </div>
</Link>

</div>

<!-- SPDX-License-Identifier: Apache-2.0 -->
