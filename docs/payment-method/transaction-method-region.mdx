---
id: transaction-method-region
title: Transaction Method & Region
sidebar_label: Transaction Method & Region
---

import Admonition from '@theme/Admonition';
import payments from "../../src/config/payment-method.js"
import { FaCheck } from "react-icons/fa"

## Transaction Method & Region

<Admonition type="note">
**For online web and mobile web payments.** Append the region code to the system method name. For example, Malaysia uses `MY`, so the full method name becomes `ALIPAYPLUS_MY`.
</Admonition>

### Method Availability Table

<table>
  <thead>
    <tr>
      <th>Method</th>
      <th>Region</th>
      <th>Quick Pay</th>
      <th>Web Payment</th>
      <th>Mobile Web Payment</th>
      <th>Transaction QR</th>
      <th>Retail QR</th>
    </tr>
  </thead>
  <tbody>
    {payments.map((payment, index) => (
      <tr key={index}>
        <td>{payment.method}</td>
        <td>{payment.region}</td>
        <td style={{textAlign: 'center'}}>{payment.quickPay && <FaCheck color="#22c55e" />}</td>
        <td style={{textAlign: 'center'}}>{payment.web && <FaCheck color="#22c55e" />}</td>
        <td style={{textAlign: 'center'}}>{payment.mobileWeb && <FaCheck color="#22c55e" />}</td>
        <td style={{textAlign: 'center'}}>{payment.transactionQR && <FaCheck color="#22c55e" />}</td>
        <td style={{textAlign: 'center'}}>{payment.retailQR && <FaCheck color="#22c55e" />}</td>
      </tr>
    ))}
  </tbody>
</table>

<!-- SPDX-License-Identifier: Apache-2.0 -->
